<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginEnd="8dip"
    android:layout_marginStart="8dip"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <TextView
                android:id="@android:id/title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center_vertical|start"
                android:maxLines="1"
                android:minHeight="@dimen/dialog_header_height"
                android:paddingEnd="16dip"
                android:paddingStart="16dip"
                android:text="@string/profile_picker_title"
                android:textColor="@android:color/holo_blue_light"
                android:textSize="@dimen/title_text_size" />

            <Button
                android:id="@+id/add_profile"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:layout_centerVertical="true"
                android:layout_margin="@dimen/standard_margin"
                android:layout_toLeftOf="@+id/delete_profile"
                android:background="@drawable/add_profile"
                android:clickable="true" />

            <ToggleButton
                android:id="@+id/delete_profile"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:layout_alignParentRight="true"
                android:layout_centerVertical="true"
                android:layout_margin="@dimen/standard_margin"
                android:background="@drawable/delete_profile"
                android:clickable="true"
                android:textOff="@null"
                android:textOn="@null" />

        </RelativeLayout>

        <View
            android:id="@+id/titleDivider"
            android:layout_width="match_parent"
            android:layout_height="@dimen/separator_width"
            android:background="@android:color/holo_blue_light" />

    </LinearLayout>

    <ListView
        android:id="@+id/profile_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

</LinearLayout>
